camunda.bpm.admin-user:
  id: admin
  password: admin
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camunda?sendFractionalSeconds=false&autoReconnect=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
camunda:
  bpm:
    database:
      type: mysql
    authorization:
      enabled: true